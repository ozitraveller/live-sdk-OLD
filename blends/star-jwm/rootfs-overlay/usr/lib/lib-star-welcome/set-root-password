clear

# echo "  -----------------------------[ screen $STEP of $STEPS ]----------------------------
echo "

  SET ROOT PASSWORD
  -----------------------
  Suggest setting the root password. This script will now execute the command:

      sudo passwd root"

if prompt "  Would you like to set the root password?"; then
	sudo passwd root

fi
